let x=5;
let y=2;
    // if typeof(x) != “number”){
    //     prompt(“not a number)};
    // if typeof(y) != "number"){
    //     prompt(“not a number)};
    if(x>y) {
	    alert(“x is the bigger number”) }
    else if(y>x) {
	    alert(“y is the bigger number”)}
    else(x=y) {
	    alert(“the two numbers are equal”)}